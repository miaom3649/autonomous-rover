# 临时实现方案：语义建图、传感器融合与语义导航

> 本文件记录当前讨论形成的方案，属于临时设计稿，后续实现和实验中可以继续修改。

## 1. 总体目标

在现有 ROS 2 架构上完成三个阶段：

1. **语义地图**：在二维 SLAM 地图中记录物体类别、位置和唯一 ID，并避免重复计数。
2. **相机-LiDAR 融合**：用 LiDAR 障碍点修正相机估计的物体位置，并比较融合前后的误差。
3. **语义导航**：支持“前往最近的椅子”等任务，由 Nav2 规划并避障。

现有 SLAM、YOLO、相机地面投影和 Nav2 保留，在其上增加三个独立职责：

```text
Camera -> YOLO -> object_localizer -> semantic_mapper -> semantic_navigation -> Nav2
                   ^                    |
LiDAR /scan -------+                    +-> Dashboard / persistent storage
```

- `object_localizer`：计算 camera-only 坐标，并在附近匹配 LiDAR 点簇。
- `semantic_mapper`：候选确认、去重、唯一 ID、位置更新和对象生命周期管理。
- `semantic_navigation`：选择语义目标，在物体旁寻找安全停车点并调用 Nav2。
- `dashboard_node`：只负责显示、控制和调试，不再承担核心语义状态管理。

## 2. 语义对象生命周期

所有对象可保存在同一个 SQLite 数据库中，用状态区分：

```text
candidate -> confirmed -> missing -> inactive/archive
```

- 第一次检测只建立 `candidate`，不立即写入正式语义地图。
- 同类对象在相近地图坐标重复出现时，更新已有记录，不创建新 ID。
- 多帧检测稳定后变为 `confirmed`，位置使用加权平均或滤波更新。
- 未确认普通候选约 30 秒未再次出现时清理。
- person 等动态对象约 5 秒未看到时从实时地图移除。
- 已确认的 chair/table 等静态对象不按时间直接删除。
- 只有小车重新观察其原位置，并连续多次确认该处为空，才标为 `missing`；多次复查仍为空后归档。

建议字段：

```text
id, class, x, y, status, confidence,
camera_confirmed, lidar_confirmed,
observation_count, miss_count, first_seen, last_seen
```

置信度由多项证据累计：YOLO 分数、多帧重复检测、地图位置稳定、多视角一致、LiDAR 匹配等。单帧检测或位置不稳定则保持候选状态。

## 3. 相机与 LiDAR 融合

采用符合现有架构的实现：

1. YOLO 检测物体。
2. 用现有图像-地面投影计算理论 `map` 坐标。
3. 在理论坐标附近搜索当前 LiDAR 障碍点。
4. 对附近点进行简单聚类，使用最近点簇的中位数或中心修正物体位置。
5. 将结果交给 `semantic_mapper`。

需要让检测结果携带原始图像时间戳，并使用该时刻的 TF 和匹配的 `/scan`，避免用异步 YOLO 返回时的最新位姿造成误差。

### LiDAR 未匹配到物体时

- 只检测一两次：保留为临时候选，不入库、不影响导航。
- 相机多次稳定检测：可作为低置信度 `camera-only` 对象入库。
- 如果该类别会影响行驶，将其发布为独立的视觉障碍供 Nav2 costmap 使用。
- 不伪造 `/scan` 激光点，不把视觉障碍写入 SLAM 几何地图，以免污染建图和 rf2o。

三类数据必须分开：

```text
SLAM map       = 真实 LiDAR 构成的静态几何地图
Nav2 costmap   = 实时 LiDAR、人员和可信视觉障碍
Semantic DB    = 对象类别、ID、位置、关系和历史状态
```

## 4. 堆叠物品

检测框重叠不能证明 A 在 B 上，因为也可能只是前后遮挡。

- 位于地面并承担占地的 B（如桌子）可以投影到二维地图并作为障碍。
- 疑似位于 B 上的 A（如瓶子）不能直接用框底投影为地面障碍。
- A 先作为候选，利用深度、接触位置、多角度观察和类别常识累积证据。
- 证据足够时记录关系 `A is_on B`；证据不足时不加入二维障碍地图。
- 现有单目深度只能辅助判断，不能将单张图的结果当成确定事实。

## 5. 建图与日常运行分离

- **建图模式**：用户遥控探索，slam_toolbox 持续更新地图并由质量监控器保存检查点。YOLO 只显示检测框，不记录语义坐标。
- **地图就绪**：用户完成并保存几何地图，系统等待用户确认进入工作模式。
- **工作/定位导航模式**：加载确认过的固定地图后，才允许 YOLO 将物体写入语义数据库；实时变化只进入 costmap 和语义数据库。

正式地图应和语义数据库绑定同一个 `map_id`，避免旧语义坐标叠加到另一张地图。

## 6. 简化的建图质量与检查点流程

第一版由用户手动遥控建图，不实现自动探索。

```text
开始建图
  -> 持续监控 SLAM
  -> 状态可信时，周期性覆盖“上一可信检查点”
  -> 检测异常时锁住电机并等待 LiDAR/SLAM 缓冲恢复
     -> 恢复：用户确认后解锁并继续建图
     -> 未恢复：终止本轮有效建图并通知用户
  -> 解锁电机，让用户遥控返回上一检查点附近
  -> 加载检查点 pose graph，重新定位并继续建图
```

只保留最新一份可信检查点。检查点至少包含：

- slam_toolbox 序列化 pose graph（用于继续建图）；
- 可视化地图 `.yaml/.pgm`；
- 检查点位置、朝向、时间、`session_id` 和 `map_id`。

异常恢复失败后不必冻结 slam_toolbox，因为回到检查点后会重新加载可信状态。但必须把当前地图标为 `INVALID`：

- 回程期间不能更新语义数据库；
- 不能建立新的可信检查点；
- 不能用错误地图执行 Nav2 自动导航；
- 回程数据只写异常日志，重新加载检查点时全部丢弃；
- LiDAR、摄像头、超声波和手动驾驶继续可用。

第一版异常依据保持简单，并要求连续异常而非单次触发：

- `POSE_JUMP`：短时间内位置突跳；
- `YAW_JUMP`：短时间内朝向突跳；
- `SCAN_TIMEOUT`：LiDAR 数据中断；
- `TF_TIMEOUT`：地图坐标变换中断；
- `MAP_TIMEOUT`：建图期间地图停止更新；
- `VALID_SCAN_TOO_LOW`：有效扫描点持续过少；
- `RECOVERY_TIMEOUT`：等待后仍未恢复。

日志措辞应为“建图状态不可信”，并记录实际观测、阈值和持续时间，不能无证据地断言地图一定丢失。

## 7. 建图日志与 Dashboard 回放

每次建图建立独立会话目录：

```text
mapping_sessions/<session_id>/
  summary.json
  full_log.jsonl
  checkpoint.posegraph
  checkpoint.yaml
  checkpoint.pgm
```

### `summary.json`

供 Dashboard 快速加载当前或上一次运行：

- 小车轨迹；
- 健康状态点；
- 检查点；
- 可疑/异常点；
- 建图终止点；
- 每个点的简短原因、阈值和执行动作。

显示约定：绿色路线/健康点、蓝色检查点、黄色可疑点、红色异常点、红色叉号表示终止位置。鼠标悬停时显示时间、坐标、状态、判定原因、测量值、阈值、LiDAR/TF 状态和系统动作。

### `full_log.jsonl`

保存完整调试信息：原始位姿、位姿变化、有效扫描点、scan/map/TF 数据年龄、地图占用格变化、全部阈值、连续失败次数、检查点信息和系统动作。Dashboard 提供下载，但绘图默认只加载摘要，避免拖慢树莓派。

## 8. Dashboard 建图控制

第一版按钮：

```text
[开始新建图] [从检查点继续] [完成并保存] [终止建图]
[当前记录] [上次记录] [下载完整日志]
```

建图时用户负责选择路线；系统负责质量检查、电机安全锁、检查点、恢复流程和日志。检测异常时，运动锁应位于 `mode_controller`，从而同时截断手动和自动速度命令。恢复失败后系统自动进入回程状态并解除电机锁，不需要用户额外确认；用户回到检查点附近后点击“从检查点继续”。

## 9. 语义导航

收到“去最近的 chair”后：

1. 查询所有可信且有效的 chair。
2. 根据当前 `map -> base_link` 位姿选择最近对象。
3. 不把椅子中心直接作为目标；在其周围选择可通行的安全停车点。
4. 让车头朝向目标并调用 `NavigateToPose`。
5. LiDAR costmap、视觉障碍和超声波共同负责避障。

## 10. 推荐实施顺序

1. 建图会话、完整日志、Dashboard 轨迹与异常回放。
2. SLAM 质量监控、电机锁和最新可信检查点恢复。
3. 检测结果时间戳同步及独立 `object_localizer`。
4. LiDAR 点簇匹配和 camera-only 回退逻辑。
5. `semantic_mapper` 数据库、生命周期和 Dashboard 展示。
6. 视觉障碍接入 Nav2 costmap。
7. `semantic_navigation` 和“前往最近物体”。
8. camera-only 与 camera+LiDAR 的定量定位实验。
