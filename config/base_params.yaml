drive_node:
  ros__parameters:
    max_linear_vel: 0.5
    wheelbase_m: 0.10  # PLACEHOLDER — measure the real front/rear axle distance
    max_motor_speed: 50
    max_steering_angle: 30.0
    cmd_timeout: 0.5
    ultrasonic_rate_hz: 5.0
    # 1 = j turns left (default ROS convention)
    # -1 = j turns right (flip if steering is reversed)
    steering_sign: -1
